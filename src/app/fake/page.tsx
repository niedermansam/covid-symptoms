import { addCaseToDb } from "@/server/api/root";
import React from "react";
import { createFakeCase } from "./createFakeCase";

function Page() {
  return (
    <div>
        
    </div>
  );
}

export default Page;
